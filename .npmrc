# @cloudflare/next-on-pages pulls wrangler@4 (peers workers-types@5) alongside
# its own workers-types@4 peer, which trips npm's strict peer resolver.
# Required for `npx @cloudflare/next-on-pages@1` to install both locally and
# in the Cloudflare Pages CI build environment.
legacy-peer-deps=true
